apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: stayly
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/part-of: stayly
    tier: application
spec:
  selector:
    app: backend
  ports:
    - name: http
      port: 4000
      targetPort: http
  type: ClusterIP